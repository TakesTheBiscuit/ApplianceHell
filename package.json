{
  "name": "appliancehell",
  "version": "0.1.0",
  "description": "Appliance hell the game",
  "main": "index.js",
  "scripts": {
    "start": "http-server",
    "release": "rm dist/itchio.zip | zip -r dist/itchio.zip * -x \".git/*\" -x \"package.json\" -x \"package-lock.json\" -x \"dist/.gitkeep\" -x \"dist/*\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TakesTheBiscuit",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.52.0"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}